// Simple test: function called many times via bytecode
fun add(a, b) {
    return a + b;
}

var total = 0;
for (i = 0; i < 200; i = i + 1) {
    total = add(total, 1);
}
print("total = " + toString(total));
